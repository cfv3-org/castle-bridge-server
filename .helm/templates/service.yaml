apiVersion: v1
kind: Service
metadata:
  name: {{ include "castle_bridge_server.fullname" . }}
  labels:
    {{- include "castle_bridge_server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "castle_bridge_server.selectorLabels" . | nindent 4 }}
