apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "castle_bridge_server.fullname" . }}-test-connection"
  labels:
    {{- include "castle_bridge_server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "castle_bridge_server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
